import 'package:flutter/material.dart';

class DayWiseExercise extends StatefulWidget {
  @override
  _DayWiseExerciseState createState() => _DayWiseExerciseState();
}

class _DayWiseExerciseState extends State<DayWiseExercise> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Day wise')),
    );
  }
}
